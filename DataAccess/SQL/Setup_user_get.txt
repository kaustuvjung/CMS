Select u.Id, up.PermissionId As PermissionId,
		u.WardId,
		u.Name as Name , u.Username, u.Email, u.WardId, u.DepartmentId, u.MobileNumber
	from dbo.[User] as u
	INNER JOIN dbo.UserPermission As up on u.Id = up.UserId
	where IsActive =  1 AND  IsStatic =  0